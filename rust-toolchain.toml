[toolchain]
channel = "stable"
components = ["rustfmt", "clippy", "rust-src", "rust-analyzer"]
profile = "minimal"

# https://internals.rust-lang.org/t/bundle-zig-cc-in-rustup-by-default/22096
# In the future, Rust might just ship with zig by default!
