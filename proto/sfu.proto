syntax = "proto3";

// protocol between the client and server via a data channel 
package sfu;

message ClientMessage {
  oneof message {
    Empty empty = 1;
  }
}

message ServerMessage {
  oneof message {
    Empty empty = 1;
  }
}

message Empty {}
