[package]
name = "pulsebeam-simulator"
publish = false
version.workspace = true
edition.workspace = true
homepage.workspace = true
documentation.workspace = true
repository.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
tokio.workspace = true
tokio-util.workspace = true
turmoil.workspace = true

proptest = "1.9.0"
pulsebeam = { version = "0.3.1", path = "../pulsebeam" }
pulsebeam-runtime = { version = "0.1.0", path = "../pulsebeam-runtime", features = ["turmoil"] }
pulsebeam-agent = { version = "0.1.0", path = "../pulsebeam-agent", features = ["turmoil"] }
test-strategy = "0.4.3"
