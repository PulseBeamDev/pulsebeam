[package]
name = "pulsebeam-agent-core"
version = "0.1.0"
edition = "2024"

[lints]
workspace = true

[dependencies]
thiserror = { version = "2.0.20", default-features = false }
proto = { package = "pulsebeam-proto", version = "0.3.2", path = "../pulsebeam-proto" }
