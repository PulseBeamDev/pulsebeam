# `pulsebeam-rtc`

Reserved workspace crate for a future portable RTC boundary. It currently
contains only scaffold code and has no production consumers or stable API.

Do not build new abstractions on this crate until its ownership and contract
are defined. If scaffold code changes, run `cargo test -p pulsebeam-rtc` and
the root `just check` gate.
