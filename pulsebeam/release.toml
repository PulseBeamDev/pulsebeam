pre-release-hook = [
  "git-cliff",
  "--include-path",
  "./pulsebeam-core/**/*",
  "--include-path",
  "./pulsebeam-proto/**/*",
  "--include-path",
  "./pulsebeam-runtime/**/*",
  "--include-path",
  "./pulsebeam/**/*",
  "-o",
  "CHANGELOG.md",
  "--tag",
  "{{version}}"
]
# pre-release-replacements = [
#   { file = "./CHANGELOG.md", search = "## \\[unreleased\\]", replace = "## [{{version}}] - {{date}}" },
# ]
tag = true
