# PulseBeam documentation

The official PulseBeam documentation site, built with VitePress.

```bash
just dev
```

Run `just check` to build the static site and validate links before submitting a
change.
