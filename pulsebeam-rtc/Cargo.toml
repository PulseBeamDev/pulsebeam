[package]
name = "pulsebeam-rtc"
version.workspace = true
authors.workspace = true
edition.workspace = true
documentation.workspace = true
homepage.workspace = true
repository.workspace = true
license.workspace = true

[dependencies]
is = { git = "https://github.com/PulseBeamDev/str0m.git", branch = "patch/0.23.1", default-features = false }
# TODO: Replace str0m's allocating SRTP/SRTCP output APIs with the reviewed
# caller-buffer API when the fork can maintain that semantic delta safely.
str0m = { git = "https://github.com/PulseBeamDev/str0m.git", branch = "patch/0.23.1", default-features = false, features = ["aws-lc-rs"] }
thiserror.workspace = true

[lints]
workspace = true
